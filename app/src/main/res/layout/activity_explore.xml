<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_explore_gradient">

    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="EXPLORAR"
        android:textAllCaps="true"
        android:textColor="@color/tamats_pink"
        android:textSize="14sp"
        android:layout_marginStart="20dp"
        android:layout_marginTop="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <!-- Eliminada la tarjeta de perfil del encabezado para que Explore muestre solo publicaciones -->

    <!-- Contenedor scrollable para publicaciones de otros usuarios -->
    <ScrollView
        android:id="@+id/svPosts"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        app:layout_constraintTop_toBottomOf="@id/tvTitle"
        app:layout_constraintBottom_toTopOf="@+id/bottomContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp">

        <LinearLayout
            android:id="@+id/llPostsContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <LinearLayout
        android:id="@+id/actionButtons"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="90dp"
        app:layout_constraintBottom_toTopOf="@+id/bottomContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Botón izquierdo: corazón (verde) -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabLike"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginEnd="20dp"
            app:fabSize="mini"
            android:src="@drawable/ic_favorite"
            app:tint="@color/white"
            app:backgroundTint="#8EE6B2"
            app:useCompatPadding="true"
            android:contentDescription="like" />

        <!-- Botón central: equis (rojo) - ancho/ovalado (FrameLayout + ImageView) -->
        <FrameLayout
            android:id="@+id/fabReject"
            android:layout_width="92dp"
            android:layout_height="64dp"
            android:background="@drawable/bg_reject_oval"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:src="@drawable/ic_close"
                app:tint="@color/white"
                android:contentDescription="reject"/>

        </FrameLayout>

        <!-- Botón derecho: estrella (azul) -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabStar"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginStart="20dp"
            app:fabSize="mini"
            android:src="@drawable/ic_star"
            app:tint="@color/white"
            app:backgroundTint="#7EA8FF"
            app:useCompatPadding="true"
            android:contentDescription="super like" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/bottomContainer"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <include layout="@layout/layout_bottom_nav" />

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
